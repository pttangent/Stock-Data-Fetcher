openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: stocks
    description: Stock data operations
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /stocks/history:
    get:
      operationId: getStockHistory
      tags: [stocks]
      summary: Get stock historical data
      description: Fetch OHLCV historical data for a stock via Yahoo Finance
      parameters:
        - name: symbol
          in: query
          required: true
          schema:
            type: string
          description: Stock ticker symbol (e.g. AAPL, TSLA, 2330.TW)
        - name: period
          in: query
          required: true
          schema:
            type: string
            enum: [1d, max]
          description: Time range - "1d" for today, "max" for full history
        - name: interval
          in: query
          required: true
          schema:
            type: string
            enum: [1m, 5m, 15m, 1h, 1d]
          description: Data interval/resolution
      responses:
        "200":
          description: Historical data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StockHistoryResult"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /stocks/info:
    get:
      operationId: getStockInfo
      tags: [stocks]
      summary: Get stock metadata/info
      description: Fetch basic metadata for a stock symbol
      parameters:
        - name: symbol
          in: query
          required: true
          schema:
            type: string
          description: Stock ticker symbol
      responses:
        "200":
          description: Stock info
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StockInfo"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /stocks/batch-summary:
    post:
      operationId: batchStockSummary
      tags: [stocks]
      summary: Batch today summary for a list of symbols
      description: >
        For each symbol, fetches today's 1m intraday bars (including pre/post market),
        computes regular-session VWAP, % of minutes above VWAP, latest price,
        after-hours price, and daily OHLCV. Uses US/Eastern timezone for session logic.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchSummaryRequest"
      responses:
        "200":
          description: Batch summary results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchSummaryResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    OHLCVBar:
      type: object
      properties:
        date:
          type: string
          description: ISO timestamp
        open:
          type: ["number", "null"]
        high:
          type: ["number", "null"]
        low:
          type: ["number", "null"]
        close:
          type: ["number", "null"]
        volume:
          type: ["integer", "null"]
      required:
        - date
    StockHistoryResult:
      type: object
      properties:
        symbol:
          type: string
        period:
          type: string
        interval:
          type: string
        rowCount:
          type: integer
        data:
          type: array
          items:
            $ref: "#/components/schemas/OHLCVBar"
        fetchedAt:
          type: string
          description: ISO timestamp when data was fetched
      required:
        - symbol
        - period
        - interval
        - rowCount
        - data
        - fetchedAt
    StockInfo:
      type: object
      properties:
        symbol:
          type: string
        shortName:
          type: ["string", "null"]
        longName:
          type: ["string", "null"]
        currency:
          type: ["string", "null"]
        exchange:
          type: ["string", "null"]
        regularMarketPrice:
          type: ["number", "null"]
        regularMarketChangePercent:
          type: ["number", "null"]
        marketCap:
          type: ["number", "null"]
        fiftyTwoWeekHigh:
          type: ["number", "null"]
        fiftyTwoWeekLow:
          type: ["number", "null"]
      required:
        - symbol
    BatchSummaryRequest:
      type: object
      properties:
        symbols:
          type: array
          items:
            type: string
          minItems: 1
          maxItems: 100
          description: List of ticker symbols to query
        date:
          type: ["string", "null"]
          description: Trade date YYYY-MM-DD (ET). Defaults to today in US/Eastern.
      required:
        - symbols
    SymbolSummaryItem:
      type: object
      properties:
        symbol:
          type: string
        shortName:
          type: ["string", "null"]
        exchange:
          type: ["string", "null"]
        currency:
          type: ["string", "null"]
        dayOpen:
          type: ["number", "null"]
        dayHigh:
          type: ["number", "null"]
        dayLow:
          type: ["number", "null"]
        dayClose:
          type: ["number", "null"]
        dayVolume:
          type: ["integer", "null"]
        dayReturnPct:
          type: ["number", "null"]
          description: (close/open - 1) * 100
        latestPrice:
          type: ["number", "null"]
          description: Most recent price (may be after-hours)
        latestBarTimeEt:
          type: ["string", "null"]
          description: Timestamp of the latest 1m bar in ET
        isAfterHours:
          type: ["boolean", "null"]
          description: True if the latest bar is after regular session close
        isPremarket:
          type: ["boolean", "null"]
          description: True if the latest bar is before regular session open
        postMarketPrice:
          type: ["number", "null"]
          description: After-hours price from Yahoo quote fields
        preMarketPrice:
          type: ["number", "null"]
          description: Pre-market price from Yahoo quote fields
        afterHoursPrice:
          type: ["number", "null"]
          description: Best available after-hours price (1m bar or quote fallback)
        afterHoursVsDayClosePct:
          type: ["number", "null"]
          description: (afterHoursPrice / dayClose - 1) * 100
        regularBarCount:
          type: ["integer", "null"]
          description: Number of 1m bars in regular session (09:30-16:00 ET)
        intradayBarCount:
          type: ["integer", "null"]
          description: Total 1m bars including pre/post market
        intradayVwapLast:
          type: ["number", "null"]
          description: Final cumulative VWAP value at end of regular session
        currentVsVwapPct:
          type: ["number", "null"]
          description: (latestPrice / intradayVwapLast - 1) * 100
        pctRegularMinutesAboveVwap:
          type: ["number", "null"]
          description: Percent of regular-session 1m bars where close > VWAP
        pctAfterHoursAboveVwap:
          type: ["number", "null"]
          description: Percent of after-hours bars where close > regular-session VWAP
        fetchError:
          type: ["string", "null"]
          description: Error message if this symbol failed to fetch
      required:
        - symbol
    BatchSummaryResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: "#/components/schemas/SymbolSummaryItem"
        tradeDate:
          type: string
          description: The trade date used for this query (ET)
        fetchedAt:
          type: string
          description: ISO timestamp when the batch completed
      required:
        - results
        - tradeDate
        - fetchedAt
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
