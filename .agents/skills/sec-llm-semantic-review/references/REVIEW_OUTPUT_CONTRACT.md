# LLM Review Output Contract

## Write boundary

The LLM must not directly update these authoritative tables:

- `filing`
- `filing_document`
- `filing_section`
- `filing_table`
- `xbrl_fact`
- `form13f_holding`
- `evidence_snippet`
- existing deterministic `semantic_assertion` rows
- existing deterministic `company_relation` rows

The reviewer emits append-only JSONL records to a staging file. A separate validator or human approval step may later promote an approved correction.

Recommended staging path:

```text
data/sec_yfinance/reviews/<run_id>/<batch_id>.jsonl
```

## One-record rule

Each line represents one atomic review decision for one primary evidence item and, where applicable, one reviewed assertion or relation.

Do not return markdown around JSONL. Do not combine several predicates, counterparties, periods, or product states into one record.

## Required fields

```json
{
  "schema_version": "sec-llm-review-v1",
  "review_id": "stable deterministic or UUID review identifier",
  "run_id": "pipeline or review run identifier",
  "batch_id": "review batch identifier",
  "reviewed_assertion_id": null,
  "reviewed_relation_id": null,
  "candidate_reason": "generic_relation_missing_scope",
  "primary_evidence_id": "evidence identifier",
  "corroborating_evidence_ids": [],
  "security_id": "security identifier",
  "issuer_id": "issuer identifier",
  "symbol": "AMD",
  "filing_id": "filing identifier",
  "accession": "0000002488-25-000012",
  "form": "10-K",
  "signal_timestamp": "2026-01-01T15:00:00Z",
  "source_available_at": "2025-02-05T21:42:57Z",
  "source_available_at_precision": "datetime",
  "action": "supersede",
  "claim_class": "reported_fact",
  "subject_key": "security:AMD",
  "predicate": "foundry_for",
  "object": {
    "target_entity": "Taiwan Semiconductor Manufacturing Company",
    "target_ticker": "TSM",
    "product_scope": "HPC, FPGA, and Adaptive SoC wafer production"
  },
  "explicitness": "explicit",
  "confidence": 0.96,
  "status": "review_required",
  "effective_from": null,
  "effective_to": null,
  "delta_class": null,
  "rationale": "The issuer explicitly identifies TSMC as a wafer foundry for the stated product groups; the generic manufacturer relation should be replaced by a scoped foundry relation.",
  "limitations": [],
  "reviewer": {
    "provider": "local",
    "model": "model-name",
    "model_version": "version-or-checkpoint",
    "temperature": 0
  },
  "prompt_hash": "sha256 of system plus task prompt",
  "taxonomy_version": "version identifier",
  "created_at": "2026-07-21T00:00:00Z"
}
```

## Allowed enums

### `action`

```text
accept
reject
supersede
create_candidate
no_change
taxonomy_candidate
```

### `claim_class`

```text
reported_fact
management_estimate
risk_hypothesis
policy_or_rule
management_interpretation
third_party_statement
reviewer_inference
```

### `explicitness`

Use the same values as the project database:

```text
explicit
estimated
inferred
```

A third-party statement may still be `explicit`; its source meaning is carried by `claim_class = third_party_statement`.

### `status`

LLM-created records must always be:

```text
review_required
```

The LLM is not authorized to emit `accepted` as a database status, even when `action = accept`. `action = accept` means it recommends retaining the deterministic claim.

### `source_available_at_precision`

```text
datetime
date
derived
```

### `delta_class`

```text
new
removed
intensified
de_intensified
unchanged
wording_only
not_comparable
null
```

## Field rules

### Identity

- `security_id`, `issuer_id`, `filing_id`, accession, and symbol must match the primary evidence row.
- The reviewer must not normalize identity by guessing.
- Anonymous entities remain anonymous stable keys.
- A counterparty mentioned in one issuer's filing does not become an issuer-side fact for that counterparty.

### Candidate reason

`candidate_reason` is mandatory and must identify why deterministic extraction was insufficient, for example:

```text
product_lifecycle_state
mixed_actual_estimate_risk
relation_role_refinement
relation_scope_missing
disclosure_delta
segment_comparability
legal_remedy_decomposition
taxonomy_candidate
false_positive_correction
```

### Evidence

- `primary_evidence_id` is mandatory.
- `corroborating_evidence_ids` may contain only governed evidence supplied within the PIT boundary.
- Do not paste or rewrite the evidence snippet into the output; reference the stored evidence ID.
- `source_available_at` must exactly match the governed evidence record and must not be generated by the LLM.
- Do not add current web evidence to this project review batch.

### Timing

- `source_available_at <= signal_timestamp` is mandatory.
- Date-precision evidence is invalid for intraday review.
- `effective_from` may describe an earlier economic period, but it never changes source availability.
- `created_at` is review creation time, not evidence availability.

### Actions

- `accept`: requires `reviewed_assertion_id` or `reviewed_relation_id`; predicate and object should normally match the reviewed record.
- `reject`: requires a reviewed record ID and a concrete rejection reason.
- `supersede`: requires a reviewed record ID and a complete corrected atomic claim.
- `create_candidate`: requires no reviewed record, but needs a supported predicate/object.
- `no_change`: predicate and object may be null; rationale must state why evidence is insufficient.
- `taxonomy_candidate`: object must contain proposed label, definition, parent, aliases, and collision notes.

### Confidence

- Confidence is numeric in `[0, 1]`.
- It measures evidence-to-semantics support, not future economic truth.
- New inferred claims below `0.50` are prohibited; use `no_change`.
- Any result below `0.90` requires challenger review.

### Rationale

- Maximum 600 characters.
- Explain the semantic decision, not investment implications.
- Do not cite future outcomes.
- Do not claim the source is economically true merely because it was filed with the SEC.

### Limitations

Use short machine-readable labels, for example:

```text
anonymous_counterparty
management_claim_unverified
scope_ambiguous
period_not_comparable
relation_direction_uncertain
unit_or_scale_uncertain
issuer_side_evidence_missing
future_state_not_observed
```

## Supersession policy

A correction never edits or deletes the original assertion or relation.

A promotion process should create:

1. a review record linked to the original assertion/relation;
2. a new candidate record linked to the same evidence or additional eligible evidence;
3. a supersession edge or status change applied at review/approval time;
4. preserved original `available_at`, extractor ID, and extractor version.

Historical queries before correction must still reproduce the original knowledge and extraction state.

## Batch manifest

Each review batch should also produce one manifest:

```json
{
  "schema_version": "sec-llm-review-batch-v1",
  "run_id": "...",
  "batch_id": "...",
  "signal_timestamp": "...",
  "candidate_query_hash": "...",
  "candidate_reason_counts": {},
  "input_assertion_count": 100,
  "input_relation_count": 10,
  "input_evidence_count": 100,
  "output_record_count": 100,
  "action_counts": {},
  "model": {},
  "prompt_hash": "...",
  "taxonomy_version": "...",
  "source_database_sha256": "...",
  "started_at": "...",
  "completed_at": "...",
  "errors": []
}
```

## Validation gate

Reject the entire batch when any of these is non-zero:

```text
missing_primary_evidence
unknown_evidence_id
identity_mismatch
source_time_mismatch
evidence_after_signal_timestamp
intraday_date_precision_use
invalid_enum
accepted_status_emitted
missing_candidate_reason
missing_model_identity
missing_prompt_hash
multi_claim_record
future_outcome_reference
external_enrichment_used
source_evidence_mutation
```

Individual records with resolvable semantic uncertainty may be retained as `no_change`; governance violations must fail the batch.
